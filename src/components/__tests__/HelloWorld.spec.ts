import { describe, it, expect } from 'vitest'

// import { mount } from '@vue/test-utils'

// test()
// it()과 동일
// expect()의 집합

// expect()
// 단정문을 사용하기 위한 함수

describe('HelloWorld', () => {
  it('renders properly', () => {
    expect('test')
  })
})
